import { Category } from "./category.entity";

/* eslint-disable prettier/prettier */
export class CategoryService {
    findAlll(): Promise<Category[]> {
      
    }
    findById(id: string): Promise<Category> {
      
    }

    save() {
      
    }

    remove() {
      const usuario = getUsuario();

      console.log(usuario)
    }
}